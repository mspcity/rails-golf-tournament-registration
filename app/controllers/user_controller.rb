class UserController < ApplicationController
  # def new
  #   @user = User.new
  # end

  # def create
  #   @user = User.new(user_params)
  #   if @user.save
  #     session[:user_id] = @user.user_id
  #     redirect_to tournaments_path
  #   else
  #     render :new
  #   end
  # end

  # i don't need this because using devise 
  # but then where do i need put redirect_to tournaments_path
end
